name: rust
description: "A new Dart FFI package project."
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.5.0-323.0.dev

dependencies:
  cli_config: ^0.2.0
  logging: ^1.2.0
  native_assets_cli:
    git:
      url: https://github.com/dart-lang/native.git
      path: pkgs/native_assets_cli/
  native_toolchain_rust:
    git:
      url: https://github.com/irondash/native_toolchain_rust.git
      path: native_toolchain_rust

dependency_overrides:
  native_assets_cli:
    git:
      url: https://github.com/dart-lang/native.git
      path: pkgs/native_assets_cli/

dev_dependencies:
  ffi: ^2.1.2
  ffigen: ^12.0.0
  flutter_lints: ^4.0.0
  test: ^1.24.9
